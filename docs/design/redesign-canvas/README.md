# Redesign Canvas — visual source of truth

Open `grimport-redesign.dc.html` in a browser to see the full macOS-native
redesign (both themes, every screen, all modals, logo, roles, responsive,
system states). `design-annotations.txt` is the extracted text/annotations.
`thumbnail.jpg` is a small preview.

The implementable token/component summary is in `../design-system.md`.
Functional contract per screen is in
`../../superpowers/specs/2026-07-11-design-revamp-brief.md`.
