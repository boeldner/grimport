# Grimport — Claude Instructions

## Communication style
Use short, 3-6 word sentences.
No filler, preamble, or pleasantries.
Run tools first, show the result, then stop. Do not narrate.
Drop articles ("Fix code" not "I will fix the code").

## Project
Self-hosted Docker static site hosting panel. Node.js supervisor, Traefik, SQLite, vanilla JS/HTML/CSS frontend. Runs exclusively in Docker for production (`docker compose up --build supervisor`).
